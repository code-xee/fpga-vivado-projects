# BRAM Test

A Zynq PS+PL project where an `Address_Generator` state machine sweeps a BRAM address range on a trigger pulse, while a bare-metal PS application writes a test pattern into the BRAM over AXI and reads it back to verify the contents.
