# FIFO

Two ways of simulating the same FIFO behaviour: `FIFO_GATE_LEVEL` is a structural, gate-level FIFO built from a shift register, counter, and multiplexer, while `FIFO_IP_SIMULATION` wraps and simulates Xilinx's FIFO Generator IP for comparison.
